# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Artefactos generados (../data/03_primary y ../models) — se VERSIONAN para
# deploy sin re-ingestar/re-entrenar. Para regenerarlos desde `../data/01_raw/`:
#   python -m ml.ingest    (Excel → CSVs limpios)
#   python -m ml.train     (CSVs → modelo XGBoost)

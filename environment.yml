# Entorno conda equivalente a backend/requirements.txt (+ pytest).
# Uso:  conda env create -f environment.yml && conda activate pilas
name: pilas
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - -r backend/requirements.txt
      - pytest==8.3.4
